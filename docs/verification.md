# 検証項目

- 80桁で ASCII 80文字が1行に収まる。
- 80桁で全角40文字が1行に収まる。
- ASCII と全角の混在で表示幅が80を超えない。
- 長い英単語と URL が単語途中で折り返す。
- 絵文字、異体字セレクタ、結合文字が分割されない。
- ウィンドウを狭めても固定桁の境界が変わらない。
- ウィンドウを広げても固定桁の境界が変わらない。
- 折り返し位置で上下移動、クリック、選択、行末移動が一致する。
- `soft_wrap` の既存モードの挙動が変わらない。
- 実ファイルに改行が挿入されず、保存前後で内容が一致する。

